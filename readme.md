I will update notes here later.



{{ site.title }} → site-wide title
{{ title | default: site.title }} → page-specific title overrides default
{{ description | default: site.description }} → same for description


things to add

photo gallery
small blocks library?



Admin working but site.js needs work in there, i want to show what is currently in the fields for example.