{
  "name": "eleventy-base-clean",
  "version": "1.0.0",
  "scripts": {
    "start": "npx eleventy --serve",
    "build": "npx eleventy && npx pagefind --site _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "pagefind": "^1.4.0"
  },
  "dependencies": {
    "slugify": "^1.6.6"
  }
}
