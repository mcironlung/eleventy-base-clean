# 1. Build Eleventy
npx eleventy

# 2. Copy _site contents to the branch/folder for GitHub Pages
# For example, to gh-pages branch:
git checkout --orphan gh-pages
git rm -rf .
cp -r _site/* .
git add .
git commit -m "Deploy Eleventy site"
git push origin gh-pages --force
git checkout main
